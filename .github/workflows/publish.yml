name: Build and Release macOS App

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:

jobs:
  build-macos:
    runs-on: macos-latest

    steps:
      - name: Clean runner environment
        run: |
          echo "🧹 Cleaning runner environment..."

          # Remove any existing keychains
          security list-keychains -d user | grep -o '[^"]*\.keychain' | while read keychain; do
              if [[ "$keychain" != "login.keychain" && "$keychain" != "System.keychain" ]]; then
                  echo "Removing keychain: $keychain"
                  security delete-keychain "$keychain" 2>/dev/null || true
              fi
          done

          # Clean Flutter cache
          rm -rf ~/.pub-cache 2>/dev/null || true
          rm -rf ~/flutter/.pub-cache 2>/dev/null || true

          # Clean any previous build artifacts
          rm -rf build/ 2>/dev/null || true
          rm -rf .dart_tool/ 2>/dev/null || true

          # Clean any leftover certificate files
          rm -f *.p12 *.zip *.dmg 2>/dev/null || true

          echo "✅ Runner environment cleaned"

      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version-file: pubspec.yaml
          channel: "stable"

      - name: Install dependencies
        run: flutter pub get

      - name: Import Code Signing Certificate
        env:
          MACOS_CERTIFICATE: ${{ secrets.MACOS_CERTIFICATE }}
          MACOS_CERTIFICATE_PASSWORD: ${{ secrets.MACOS_CERTIFICATE_PASSWORD }}
        run: |
          echo "🔐 Setting up code signing certificate..."
          echo "$MACOS_CERTIFICATE" | base64 --decode > certificate.p12

          echo "📱 Creating and configuring keychain..."
          security create-keychain -p "" build.keychain
          security default-keychain -s build.keychain
          security unlock-keychain -p "" build.keychain

          echo "📦 Importing certificate..."
          security import certificate.p12 -k build.keychain -P "$MACOS_CERTIFICATE_PASSWORD" -A
          security set-key-partition-list -S apple-tool:,apple:,codesign: -s -k "" build.keychain

          echo "🧹 Cleaning up certificate file..."
          rm certificate.p12

          echo "✅ Certificate setup complete. Available identities:"
          security find-identity -v -p codesigning

      - name: Configure Xcode project for CI signing
        env:
          APPLE_TEAM_ID: ${{ secrets.APPLE_TEAM_ID }}
        run: |
          chmod +x scripts/safe_xcode_config.sh
          ./scripts/safe_xcode_config.sh

      - name: Build macOS app
        run: |
          flutter build macos --release

      - name: Code sign the app
        env:
          APPLE_TEAM_ID: ${{ secrets.APPLE_TEAM_ID }}
        run: |
          APP_PATH=$(find build/macos/Build/Products/Release -name "*.app")

          # Get signing identity
          IDENTITY=$(security find-identity -p codesigning -v | grep "Developer ID Application" | head -n1 | awk -F\" '{print $2}')
          echo "Using identity: $IDENTITY"

          # Sign frameworks first
          echo "Signing frameworks..."
          for framework in "$APP_PATH/Contents/Frameworks"/*.framework; do
              if [ -d "$framework" ]; then
                  echo "Signing $framework"
                  codesign --force --options runtime --sign "$IDENTITY" "$framework"
              fi
          done

          # Sign main binary
          echo "Signing main binary..."
          BINARY_PATH=$(find "$APP_PATH/Contents/MacOS" -type f -perm +111 | head -1)
          codesign --force --options runtime --sign "$IDENTITY" "$BINARY_PATH"

          # Sign app bundle
          echo "Signing app bundle..."
          codesign --deep --force --verify --verbose=2 --options runtime --sign "$IDENTITY" "$APP_PATH"

          # Verify signature (but not with spctl yet - that requires notarization)
          echo "Verifying code signature..."
          codesign --verify --deep --strict --verbose=2 "$APP_PATH"
          echo "✅ Code signature valid (notarization still required for spctl verification)"

      - name: Create zip for notarization
        run: |
          APP_PATH=$(find build/macos/Build/Products/Release -name "*.app")
          APP_NAME=$(basename "$APP_PATH" .app)
          echo "📦 Creating zip for notarization..."
          echo "App path: $APP_PATH"
          echo "App name: $APP_NAME"

          cd build/macos/Build/Products/Release
          zip -r "../../../../../$APP_NAME.zip" "$(basename "$APP_PATH")"
          cd ../../../../../

          echo "📁 Zip created: $APP_NAME.zip ($(du -h "$APP_NAME.zip" | cut -f1))"
          echo "✅ Ready for notarization"

      - name: Notarize app
        env:
          APPLE_ID: ${{ secrets.APPLE_ID }}
          APPLE_APP_SPECIFIC_PASSWORD: ${{ secrets.APPLE_APP_SPECIFIC_PASSWORD }}
          APPLE_TEAM_ID: ${{ secrets.APPLE_TEAM_ID }}
        run: |
          APP_PATH=$(find build/macos/Build/Products/Release -name "*.app")
          APP_NAME=$(basename "$APP_PATH" .app)
          ZIP_PATH="$APP_NAME.zip"

          # Verify credentials first
          echo "🔍 Verifying Apple ID and Team ID association..."
          echo "Apple ID: $APPLE_ID"
          echo "Team ID: $APPLE_TEAM_ID"

          # Test credentials with a simple history call
          if ! xcrun notarytool history --apple-id "$APPLE_ID" --team-id "$APPLE_TEAM_ID" --password "$APPLE_APP_SPECIFIC_PASSWORD" --output-format json | head -1 > /dev/null 2>&1; then
              echo "❌ Failed to verify credentials. Common issues:"
              echo "1. Apple ID '$APPLE_ID' is not associated with Team ID '$APPLE_TEAM_ID'"
              echo "2. App-specific password is incorrect"
              echo "3. Team ID format is wrong (should be 10 characters like 'ABC1234DEF')"
              echo ""
              echo "To debug:"
              echo "1. Go to https://developer.apple.com/account"
              echo "2. Verify your Team ID under 'Membership' section"
              echo "3. Ensure the Apple ID has access to this team"
              echo "4. Regenerate app-specific password at https://appleid.apple.com"
              exit 1
          fi

          echo "✅ Credentials verified successfully"

          # Submit to Apple Notary service
          echo "📤 Submitting to Apple Notary service..."
          xcrun notarytool submit "$ZIP_PATH" \
              --apple-id "$APPLE_ID" \
              --team-id "$APPLE_TEAM_ID" \
              --password "$APPLE_APP_SPECIFIC_PASSWORD" \
              --wait

          # Staple notarization ticket
          echo "📎 Stapling notarization ticket..."
          xcrun stapler staple "$APP_PATH"
          echo "✅ Stapling complete"

          # Verify stapled app and Gatekeeper approval
          echo "🛡️ Verifying Gatekeeper approval..."
          if spctl --verbose=4 --assess --type execute "$APP_PATH"; then
              echo "✅ SUCCESS: App is approved by macOS Gatekeeper!"
              echo "🔒 Users will be able to run this app without security warnings"
          else
              echo "❌ FAILED: App is NOT approved by Gatekeeper"
              echo "⚠️ Users will see security warnings when trying to run this app"
              exit 1
          fi

      - name: Create DMG (optional - for direct distribution)
        run: |
          APP_PATH=$(find build/macos/Build/Products/Release -name "*.app")
          APP_NAME=$(basename "$APP_PATH" .app)
          hdiutil create -volname "$APP_NAME" -srcfolder "$APP_PATH" -ov -format UDZO "$APP_NAME.dmg"

      - name: Create Release
        uses: softprops/action-gh-release@v1
        if: startsWith(github.ref, 'refs/tags/')
        with:
          files: |
            *.dmg
            *.zip
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
